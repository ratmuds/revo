[project]
name = "revo-pc-code"
version = "0.1.0"
description = "Host communication and camera servers for revo robot arm"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pyserial>=3.5",
    "opencv-python>=4.8.0",
    "numpy>=1.24.0",
]
